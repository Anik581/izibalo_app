<div class="navbar navbar-default">
	<button class="navbar-toggle" data-toggle = "collapse" data-target = ".navContentCollapse">
		<span class="sr-only">Toggle navigation</span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
	</button>
	<div class="collapse navbar-collapse navContentCollapse">
		<ul class="nav navbar-nav navbar-right">
				<li class="nav_li"> 
		      <div class="btn-group navbar-btn">
            <%= link_to "Tasks", tasks_path, class: "btn btn-danger" %>
            <button data-toggle="dropdown" class="btn btn-danger dropdown-toggle"><span class="caret"></span></button>
            <ul class="dropdown-menu">
            	<% @tasks.each do |task| %>
            		<li><%= link_to task.name, task_path(task.id) %></li>
            	<% end %>
            </ul>
          </div>
				</li>
		</ul>
		<%= yield %>
	</div>
</div>