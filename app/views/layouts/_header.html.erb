<nav class=" navbar navbar-static-top header-style" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<%= link_to "izibalo", login_path, id: "logo" %>
		</div>

		<div class="collapse navbar-collapse">
			<div class="nav navbar-btn btn-group pull-right">
				<% if logged? %>
					<%= link_to "log out", logout_path, class: "btn btn-warning btn-nav", method: :delete %>
					<%= link_to "my stats", user_path(current_user), class: "btn btn-success btn-nav " %>
					<%= link_to "create task", new_task_path, class:"btn btn-success btn-nav " %>
					<%= link_to "edit profile", edit_user_path(current_user), class: "btn btn-success btn-nav" %>
					<%= link_to "delete account", user_path(current_user), class: "btn btn-danger btn-nav", method: :delete %>
				<% else %>
				  <% if content_for(:title).include?("Create")  %>
				 		<%= link_to "Login", login_path, class: "btn btn-success" %>
				 	<% else %>
				 		<%= link_to "Create account", new_user_path, class: "btn  btn-success" %>
				 	<% end %>
				<% end %>
			</ul>
		</div>

	</div>
</nav>
